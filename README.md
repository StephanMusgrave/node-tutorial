# Node

## Set up
$ mkdir tutorial
$ cd tutorial/
$ npm init

$ npm install --save express
$ npm install --save ejs

## To run the server:
$ node server.js

## To see output in the terminal:
$ curl -i http://localhost:3000




